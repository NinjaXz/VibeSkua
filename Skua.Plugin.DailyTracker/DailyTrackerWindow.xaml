<Window x:Class="Skua.Plugin.DailyTracker.DailyTrackerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Daily &amp; Weekly Tracker" Height="700" Width="500"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <DataTemplate x:Key="QuestCategoryTemplate">
            <Border Background="#2D2D30" CornerRadius="8" Margin="0,0,0,15" Padding="10">
                <StackPanel>
                    <DockPanel Margin="0,0,0,10">
                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="#2196F3" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                        <Button Content="Start" Width="60" Height="24" DockPanel.Dock="Right" HorizontalAlignment="Right" Click="StartCategory_Click" Tag="{Binding Name}" Visibility="{Binding HasCategoryScript, Converter={StaticResource BoolToVis}}" Background="#2196F3" Foreground="White" BorderBrush="Transparent" Cursor="Hand" FontSize="11"/>
                    </DockPanel>
                    <ItemsControl ItemsSource="{Binding Quests}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding ID}" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" TextTrimming="CharacterEllipsis" Margin="10,0" VerticalAlignment="Center"/>
                                    <Border Grid.Column="2" Background="{Binding StatusColor}" CornerRadius="4" Padding="8,4" MinWidth="80">
                                        <TextBlock Text="{Binding StatusText}" Foreground="White" FontWeight="SemiBold" TextAlignment="Center" FontSize="12"/>
                                    </Border>
                                    <Button Grid.Column="3" Content="Start" MinWidth="50" Padding="8,2" Margin="5,0" VerticalAlignment="Center" Visibility="{Binding ShowScriptVisibility}" Click="StartScript_Click" Tag="{Binding ScriptName}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Task Tracker" FontSize="24" FontWeight="Bold" DockPanel.Dock="Left" VerticalAlignment="Center"/>
            <Button Content="Refresh" Width="80" Height="30" DockPanel.Dock="Right" HorizontalAlignment="Right" Click="Refresh_Click"
                    Background="#3E3E42" Foreground="White" BorderBrush="Transparent" Cursor="Hand"/>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="CategoriesControl" ItemTemplate="{StaticResource QuestCategoryTemplate}"/>
        </ScrollViewer>
    </Grid>
</Window>
